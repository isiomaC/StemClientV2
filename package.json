{
  "name": "stemclient_2",
  "version": "1.0.0",
  "description": "inphinity client website",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.6"
  }
}
